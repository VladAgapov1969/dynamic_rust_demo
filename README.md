# 🚀 dynamic_rust_demo

Production-ready Rust concurrency patterns:
- `Arc` + `RwLock` for thread-safe shared state
- `tokio::spawn` for async I/O
- `spawn_blocking` for CPU-bound work
- `indicatif` for real-time terminal progress bars

## ▶️ Run
```bash
cargo run --release
